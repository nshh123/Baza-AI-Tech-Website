# Repository Workflow Guidelines

## Git Completion Policy
Whenever code changes, optimizations, or feature additions are completed and verified:
1. Stage relevant changed and new files (`git add`).
2. Create a clear, descriptive conventional commit message (`git commit -m "..."`).
3. Push the commits to the remote GitHub repository (`git push origin <current-branch>`).
